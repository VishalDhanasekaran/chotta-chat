import styled from "styled-components";
import Row from "@/components/common/Row";

const GetStartedEl = styled(Row)``;

function GetStarted() {
  return (
    <GetStartedEl>
        Get Started
    </GetStartedEl>
  )
}

export default GetStarted